<div class="navbar navbar-static-top" id="stickyNavbar" style="margin-bottom: 15px;">
    <div class="navbar-inner">
        <div class="container">
            <div class="row">
                <div class="span12">
                    <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <div class="nav-collapse collapse">
                        <ul class="nav" id="mainNavigation">
                            <li {% if app.request.get('_route') == 'accueil' %}class="active"{% endif %}><a href="{{ path('accueil') }}">Accueil</a></li>
                                {{ render(controller('frontAccueilBundle:Front:menu')) }}
                            <li {% if app.request.get('_route') == 'contact' %}class="active"{% endif %}><a href="{{ path('contact') }}">contact</a></li>
                        </ul>
 
                        <form class="navbar-form pull-right" action="{{ path('Filtre') }}" method="POST" />
                        <button type="submit"><span class="icon-search"></span></button>
                        <input type="text" class="span1" name="FiltreSearch" id="navSearchInput" class='required' placeholder="mot clé" />
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
                        <style>
                            #mainNavigation li
                            {
                                font-size: 11px;
                            }
                        </style>            