{% extends ':admin:base.html.twig' %}

{% block body -%}
    <nav>
        <div id="jCrumbs" class="breadCrumb module">
            <ul>{% block sitmap %}
                    <li>    
                        <a href="{{ path("admin_general_homepage") }}"><i class="icon-home"></i></a>
                    </li>
                    <li>    
                        <a  href="{{ path("region") }}">Région</a>
                    </li>
                    <li>    
                        <a href="#">Création</a>
                    </li>
                 {% endblock %}
            </ul>
        </div>
    </nav>  

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('region') }}">
            Liste
        </a>
    </li>
</ul>
{% endblock %}
